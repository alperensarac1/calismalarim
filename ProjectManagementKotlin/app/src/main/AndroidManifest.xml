<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Retrofit ve OkHttp üzerinden backend istekleri gönderebilmek
        için internet izni gereklidir.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Cihazın internet veya ağ bağlantısının mevcut olup olmadığını
        kontrol edebilmek için kullanılır.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Android 13 ve üzerindeki cihazlarda indirme veya WorkManager
        bildirimleri göstermek için kullanılacaktır.

        Bu izni ilerleyen aşamada çalışma zamanında kullanıcıdan isteyeceğiz.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ProjectManagementApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectManagementKotlin"
        android:usesCleartextTraffic="true">

        <!--
            android:usesCleartextTraffic="true"

            Uygulamanın HTTPS dışında HTTP adreslerine de bağlanmasına
            izin verir.

            Böylece aşağıdaki geliştirme adresleri çalışacaktır:

            Gerçek cihaz:
            http://10.203.83.58:8080/

            Android Emulator:
            http://10.0.2.2:8080/
        -->

        <activity
            android:screenOrientation="portrait"
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

    </application>

</manifest>